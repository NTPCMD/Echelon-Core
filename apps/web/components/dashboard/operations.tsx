"use client";

import { motion } from "framer-motion";
import { CalendarDays, Check, ChevronLeft, ChevronRight, Clock3, Filter, Mail, MoreHorizontal, Plus, Search, SlidersHorizontal, Sparkles, UserPlus, Users, Wrench } from "lucide-react";
import { useMemo, useState } from "react";
import { Avatar } from "../ui/avatar";
import { Badge } from "../ui/badge";
import { Button } from "../ui/button";
import { Card, CardContent } from "../ui/card";
import { Input } from "../ui/input";
import { Switch } from "../ui/switch";
import { bookings, customers, services, staff } from "./data";
import { EmptyState, fadeUp, PageHeader, stagger } from "./page";

function Toolbar({ search, onSearch, placeholder, children }: { search: string; onSearch: (value: string) => void; placeholder: string; children?: React.ReactNode }) {
  return <div className="mb-4 flex flex-col gap-3 rounded-[18px] border border-white/[.06] bg-white/[.018] p-3 shadow-[inset_0_1px_0_rgba(255,255,255,.02)] sm:flex-row sm:items-center"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 size-3.5 -translate-y-1/2 text-white/20" /><Input value={search} onChange={(event) => onSearch(event.target.value)} placeholder={placeholder} className="border-white/[.055] bg-white/[.025] pl-9 focus:bg-white/[.045]" /></div><div className="flex gap-2">{children}<Button variant="outline"><Filter className="size-4" /> Filter</Button></div></div>;
}

const statusTone = (status: string) => status === "Confirmed" || status === "Completed" ? "success" as const : status === "Pending" ? "warning" as const : "danger" as const;

export function BookingsPage() {
  const [search, setSearch] = useState("");
  const filtered = bookings.filter((booking) => `${booking.customer} ${booking.service} ${booking.id}`.toLowerCase().includes(search.toLowerCase()));
  return <><PageHeader eyebrow="Operations" title="Bookings" description="Manage appointments, payments and customer requests in one place." action={<Button><Plus className="size-4" /> New booking</Button>} /><div className="mb-4 grid grid-cols-2 gap-3 lg:grid-cols-4">{[{ label: "Today", value: "12", note: "8 confirmed" }, { label: "This week", value: "68", note: "+12.5% vs last week" }, { label: "Pending", value: "6", note: "Needs attention" }, { label: "Cancellations", value: "2.1%", note: "Below 3% target" }].map((item) => <Card key={item.label}><CardContent className="p-4"><p className="text-[10px] font-bold uppercase tracking-[.1em] text-[#aaa6ae]">{item.label}</p><p className="mt-2 text-2xl font-bold tracking-[-.04em]">{item.value}</p><p className="mt-1 text-[11px] text-[#918c96]">{item.note}</p></CardContent></Card>)}</div><Toolbar search={search} onSearch={setSearch} placeholder="Search bookings…"><Button variant="outline" className="hidden sm:inline-flex"><CalendarDays className="size-4" /> 10–16 June</Button></Toolbar><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[920px] text-left"><thead className="bg-[#faf9f7]"><tr className="text-[10px] font-bold uppercase tracking-[.08em] text-[#aaa6ae]"><th className="w-10 px-5 py-3"><input type="checkbox" aria-label="Select all bookings" /></th><th className="px-3 py-3">Customer</th><th className="px-3 py-3">Service</th><th className="px-3 py-3">Team member</th><th className="px-3 py-3">Date & time</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Amount</th><th className="w-10 px-4 py-3" /></tr></thead><tbody>{filtered.map((booking) => <tr key={booking.id} className="border-t border-[#efede9] transition hover:bg-[#faf9f7]"><td className="px-5 py-4"><input type="checkbox" aria-label={`Select ${booking.id}`} /></td><td className="px-3 py-4"><div className="flex items-center gap-2.5"><Avatar initials={booking.initials} className="size-8" /><div><p className="text-xs font-semibold">{booking.customer}</p><p className="text-[10px] text-[#aaa6ae]">{booking.id}</p></div></div></td><td className="px-3 py-4"><p className="text-xs font-medium">{booking.service}</p><p className="text-[10px] text-[#aaa6ae]">{booking.duration}</p></td><td className="px-3 py-4 text-xs text-[#625e67]">{booking.staff}</td><td className="px-3 py-4 text-xs text-[#625e67]">{booking.date}</td><td className="px-3 py-4"><Badge tone={statusTone(booking.status)}>{booking.status}</Badge></td><td className="px-3 py-4 text-xs font-semibold">{booking.amount}</td><td className="px-4"><Button variant="ghost" size="icon" className="size-8"><MoreHorizontal className="size-4" /></Button></td></tr>)}</tbody></table></div>{filtered.length === 0 ? <div className="p-6"><EmptyState icon={CalendarDays} title="No bookings found" description="Try adjusting your search or filters." /></div> : <div className="flex items-center justify-between border-t border-[#efede9] px-5 py-3 text-[11px] text-[#918c96]"><span>Showing {filtered.length} of 284 bookings</span><div className="flex gap-1"><Button variant="outline" size="icon" className="size-8"><ChevronLeft className="size-3.5" /></Button><Button variant="outline" size="icon" className="size-8"><ChevronRight className="size-3.5" /></Button></div></div>}</Card></>;
}

const days = ["Mon 10", "Tue 11", "Wed 12", "Thu 13", "Fri 14", "Sat 15", "Sun 16"];
const times = ["8 AM", "9 AM", "10 AM", "11 AM", "12 PM", "1 PM", "2 PM", "3 PM", "4 PM", "5 PM"];
const events = [
  { day: 0, top: 53, height: 68, title: "Northstar Capital", service: "Product strategy", tone: "bg-[#edeaff] border-[#cfc8ff] text-[#a99cff]" },
  { day: 1, top: 112, height: 96, title: "Lumen Health", service: "Experience workshop", tone: "bg-[#fff0db] border-[#f3d39e] text-[#f8d48a]" },
  { day: 2, top: 20, height: 54, title: "Atlas Logistics", service: "AI workflow audit", tone: "bg-[#e8f5ff] border-[#b9ddf4] text-[#8ed3f5]" },
  { day: 3, top: 184, height: 64, title: "Form House", service: "Brand system review", tone: "bg-[#e7f7ef] border-[#b9e4ce] text-[#80ddb6]" },
  { day: 4, top: 78, height: 122, title: "Meridian Group", service: "AI automation sprint", tone: "bg-[#ffebef] border-[#f5c3cf] text-[#f7a4b8]" },
  { day: 5, top: 142, height: 54, title: "Aperture Group", service: "Technical advisory", tone: "bg-[#edeaff] border-[#cfc8ff] text-[#a99cff]" },
];

export function CalendarPage() {
  return <><PageHeader eyebrow="Operations" title="Calendar" description="Plan your week, manage availability and keep the team in sync." action={<div className="flex gap-2"><Button variant="outline">Today</Button><Button><Plus className="size-4" /> New booking</Button></div>} /><Card className="overflow-hidden"><div className="flex flex-wrap items-center gap-3 border-b border-[#ebe8e3] p-4"><div className="flex items-center gap-1"><Button variant="ghost" size="icon"><ChevronLeft className="size-4" /></Button><Button variant="ghost" size="icon"><ChevronRight className="size-4" /></Button></div><h2 className="text-sm font-semibold">10–16 June 2026</h2><div className="ml-auto flex items-center gap-2"><div className="hidden rounded-xl bg-[#f2f0ed] p-1 text-xs font-medium sm:flex"><button className="rounded-lg bg-white px-3 py-1.5 shadow-sm">Week</button><button className="px-3 py-1.5 text-[#89848e]">Month</button></div><Button variant="outline" size="icon"><SlidersHorizontal className="size-4" /></Button></div></div><div className="overflow-x-auto"><div className="min-w-[900px]"><div className="grid grid-cols-[64px_repeat(7,1fr)] border-b border-[#ebe8e3]"><div /><>{days.map((day, index) => <div key={day} className="border-l border-[#efede9] py-3 text-center"><p className="text-[10px] font-semibold uppercase tracking-wider text-[#aaa6ae]">{day.split(" ")[0]}</p><p className={`mx-auto mt-1 grid size-7 place-items-center rounded-full text-xs font-bold ${index === 4 ? "bg-[#6d5dfc] text-white" : ""}`}>{day.split(" ")[1]}</p></div>)}</></div><div className="grid h-[560px] grid-cols-[64px_repeat(7,1fr)]"><div className="relative">{times.map((time, index) => <span key={time} className="absolute right-3 text-[10px] text-[#aaa6ae]" style={{ top: index * 56 - 5 }}>{time}</span>)}</div>{days.map((day, dayIndex) => <div key={day} className="relative border-l border-[#efede9]">{times.map((time, index) => <div key={time} className="absolute left-0 right-0 border-t border-[#f1efec]" style={{ top: index * 56 }} />)}{events.filter((event) => event.day === dayIndex).map((event) => <motion.div key={event.title} initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} className={`absolute left-1.5 right-1.5 rounded-lg border p-2 text-[10px] shadow-sm ${event.tone}`} style={{ top: event.top, height: event.height }}><p className="truncate font-bold">{event.title}</p><p className="mt-0.5 truncate opacity-75">{event.service}</p></motion.div>)}</div>)}</div></div></div></Card></>;
}

export function ServicesPage() {
  const [search, setSearch] = useState("");
  const [active, setActive] = useState<Record<string, boolean>>(() => Object.fromEntries(services.map((service) => [service.name, service.active])));
  const filtered = services.filter((service) => service.name.toLowerCase().includes(search.toLowerCase()));
  return <><PageHeader eyebrow="Catalogue" title="Services" description="Create, price and organise the services customers can book." action={<Button><Plus className="size-4" /> Add service</Button>} /><Toolbar search={search} onSearch={setSearch} placeholder="Search services…" /><motion.div variants={stagger} initial="hidden" animate="show" className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{filtered.map((service) => <motion.div variants={fadeUp} key={service.name}><Card className="h-full transition hover:-translate-y-0.5 hover:shadow-[0_10px_30px_rgba(29,26,36,.06)]"><CardContent className="p-5"><div className="flex items-start justify-between"><span className="grid size-10 place-items-center rounded-xl bg-[#efedff] text-[#6251e7]"><Wrench className="size-4" /></span><div className="flex items-center gap-2"><Switch checked={active[service.name] ?? false} onCheckedChange={(checked) => setActive((value) => ({ ...value, [service.name]: checked }))} label={`Toggle ${service.name}`} /><Button variant="ghost" size="icon" className="size-8"><MoreHorizontal className="size-4" /></Button></div></div><Badge className="mt-5" tone="purple">{service.category}</Badge><h3 className="mt-3 text-sm font-semibold">{service.name}</h3><div className="mt-2 flex items-center gap-3 text-[11px] text-[#918c96]"><span className="flex items-center gap-1"><Clock3 className="size-3" />{service.duration}</span><span className="font-semibold text-[#343037]">{service.price}</span></div><div className="mt-5 grid grid-cols-2 border-t border-[#efede9] pt-4"><div><p className="text-[10px] text-[#aaa6ae]">Bookings</p><p className="mt-1 text-xs font-semibold">{service.bookings}</p></div><div className="border-l border-[#efede9] pl-4"><p className="text-[10px] text-[#aaa6ae]">Revenue</p><p className="mt-1 text-xs font-semibold">{service.revenue}</p></div></div></CardContent></Card></motion.div>)}</motion.div>{filtered.length === 0 ? <EmptyState icon={Wrench} title="No matching services" description="Try another search, or create a new service." action={<Button size="sm"><Plus className="size-3.5" /> Add service</Button>} /> : null}</>;
}

export function StaffPage() {
  return <><PageHeader eyebrow="Team" title="Staff" description="Manage your team, schedules, permissions and performance." action={<Button><UserPlus className="size-4" /> Invite team member</Button>} /><div className="mb-4 grid gap-3 sm:grid-cols-3"><Card><CardContent className="p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[#aaa6ae]">Team members</p><p className="mt-2 text-2xl font-bold">4</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[#aaa6ae]">Booked hours</p><p className="mt-2 text-2xl font-bold">126.5h</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[#aaa6ae]">Utilisation</p><p className="mt-2 text-2xl font-bold">78%</p></CardContent></Card></div><motion.div variants={stagger} initial="hidden" animate="show" className="grid gap-4 md:grid-cols-2">{staff.map((member) => <motion.div variants={fadeUp} key={member.name}><Card><CardContent className="p-5"><div className="flex items-start gap-3"><Avatar initials={member.initials} tone={member.tone} className="size-11" /><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h3 className="text-sm font-semibold">{member.name}</h3>{member.status === "Available" ? <span className="size-1.5 rounded-full bg-[#21a66f]" /> : null}</div><p className="mt-0.5 text-xs text-[#918c96]">{member.role}</p></div><Button variant="ghost" size="icon" className="size-8"><MoreHorizontal className="size-4" /></Button></div><div className="mt-5 rounded-xl bg-[#f8f6f3] px-3 py-2 text-[11px] text-[#716c76]"><span className="font-semibold">Today:</span> {member.status}</div><div className="mt-4 grid grid-cols-3 divide-x divide-[#ebe8e3]"><div><p className="text-[10px] text-[#aaa6ae]">Bookings</p><p className="mt-1 text-xs font-semibold">{member.bookings}</p></div><div className="pl-4"><p className="text-[10px] text-[#aaa6ae]">Rating</p><p className="mt-1 text-xs font-semibold">★ {member.rating}</p></div><div className="pl-4"><p className="text-[10px] text-[#aaa6ae]">Revenue</p><p className="mt-1 text-xs font-semibold">{member.revenue}</p></div></div></CardContent></Card></motion.div>)}</motion.div></>;
}

export function CustomersPage() {
  const [search, setSearch] = useState("");
  const filtered = useMemo(() => customers.filter((customer) => `${customer.name} ${customer.email}`.toLowerCase().includes(search.toLowerCase())), [search]);
  return <><PageHeader eyebrow="Relationships" title="Customers" description="Understand your customers and build lasting relationships." action={<div className="flex gap-2"><Button variant="outline"><Mail className="size-4" /> Send campaign</Button><Button><UserPlus className="size-4" /> Add customer</Button></div>} /><div className="mb-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[{ label: "Total customers", value: "1,429", note: "+96 this month" }, { label: "Returning rate", value: "68%", note: "+4.2% this quarter" }, { label: "Avg. customer value", value: "$286", note: "Across 6.2 visits" }, { label: "At risk", value: "34", note: "No visit in 90 days" }].map((item) => <Card key={item.label}><CardContent className="p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[#aaa6ae]">{item.label}</p><p className="mt-2 text-2xl font-bold">{item.value}</p><p className="mt-1 text-[11px] text-[#918c96]">{item.note}</p></CardContent></Card>)}</div><Toolbar search={search} onSearch={setSearch} placeholder="Search customers…" /><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead className="bg-[#faf9f7]"><tr className="text-[10px] font-bold uppercase tracking-wider text-[#aaa6ae]"><th className="px-5 py-3">Customer</th><th className="px-3 py-3">Segment</th><th className="px-3 py-3">Visits</th><th className="px-3 py-3">Total spent</th><th className="px-3 py-3">Last visit</th><th className="px-3 py-3" /></tr></thead><tbody>{filtered.map((customer, index) => <motion.tr initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: index * .04 }} key={customer.email} className="border-t border-[#efede9] hover:bg-[#faf9f7]"><td className="px-5 py-4"><div className="flex items-center gap-3"><Avatar initials={customer.initials} /><div><p className="text-xs font-semibold">{customer.name}</p><p className="mt-0.5 text-[10px] text-[#aaa6ae]">{customer.email}</p></div></div></td><td className="px-3 py-4"><Badge tone={customer.tag === "VIP" || customer.tag === "High value" ? "purple" : customer.tag === "New" ? "success" : "neutral"}>{customer.tag}</Badge></td><td className="px-3 py-4 text-xs">{customer.visits}</td><td className="px-3 py-4 text-xs font-semibold">{customer.spent}</td><td className="px-3 py-4 text-xs text-[#77727c]">{customer.lastVisit}</td><td className="px-3 py-4"><Button variant="ghost" size="icon" className="size-8"><MoreHorizontal className="size-4" /></Button></td></motion.tr>)}</tbody></table></div>{filtered.length === 0 ? <div className="p-6"><EmptyState icon={Users} title="No customers found" description="No customer matches your current search." /></div> : null}</Card></>;
}
